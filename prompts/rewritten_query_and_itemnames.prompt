历史会话：
{history_text}

当前用户问题：{query}

请根据历史会话和当前问题，完成以下任务：

1. 提取书籍名称（item_names）：
   - 如果用户明确提到了书名，请提取出来，可能有一个或多个，但不能重复
   - 如果用户使用了代词（如"这本"、"它"、"这本书"），请结合历史会话指代消解，确定书名
   - 如果无法确定书名，item_names 返回空列表

2. 提取作者名称（author_names）：
   - 如果用户明确提到了作者名，请提取出来
   - 如果无法确定，author_names 返回空列表

3. 提取类别（category）：
   - 如果用户提到了明确的类别/标签（如"科幻"、"悬疑"、"历史"、"儿童"、"教育"、"经典"、"名著"等），提取出来
   - 无法确定时为 null

4. 提取内容类型（content_type）：
   - 如果用户提到了明确的内容类型（有声书信息/书籍简介/作者介绍/听书笔记/推荐运营资料/用户评论摘要/常见问答），提取出来
   - 无法确定时为 null

5. 识别查询场景（scene），从以下选项中选择最匹配的一项：
   - recommend（书籍推荐）：用户想找某类书、根据场景/类别/标签推荐
   - detail（书籍详情）：用户想了解某本书的简介、作者、时长、演播、亮点等
   - retrieval（内容检索）：用户想定位某本书的具体内容、某个主题的解释
   - notes（听书笔记/评论）：用户想看某本书的书评、笔记、评论摘要
   - unknown（通用问答/无法判断）

6. 改写用户问题（rewritten_query），使其成为包含书名/作者的独立完整问题

请直接返回JSON格式结果：
{{
    "item_names": ["书名A", "书名B"],
    "author_names": ["作者A"],
    "category": "科幻",
    "content_type": "书籍简介",
    "scene": "recommend|detail|retrieval|notes|unknown",
    "rewritten_query": "改写后的完整问题"
}}